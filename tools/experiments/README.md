# Experiments

Store reusable diagnostic and experimental scripts here, grouped by feature when needed. Experiments must not be imported by production entry points. Put generated output in the ignored `tools/experiments/output/` directory, and use `/tmp` for disposable one-off work.
